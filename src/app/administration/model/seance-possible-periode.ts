import { ISeance } from "../../interface/seance";

export class SeancePossiblePeriode {
    date: Date;
    selectionne = false;
  
    constructor(date: Date) {
      this.date = date;
    }
    
    
}